server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP

    location /static/ {
        alias /var/www/learning_logs/staticfiles/;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/run/uwsgi/learning_logs.sock;
    }
}
